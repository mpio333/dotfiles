#!/bin/bash
VOL=$(bash /home/hellall/.local/bin/volume)
LOCALTIME=$(date '+%a %H:%M')
BAT="$(bash /home/hellall/.local/bin/battery)"
NET="$(bash /home/hellall/.local/bin/network)"

xsetroot -name "$NET $VOL $BAT $LOCALTIME"
